object NDN extends App {

  //  val calcFunc = (n: Int) => n.toString.map(_.asDigit).map(math.pow(_, n.toString.length)).sum.toInt
  //
  //  println(Iterator.from(100).filter(i => i == calcFunc(i)).takeWhile(_ < 999).toList mkString " ")

}